jobs:
- job: prep
  pool: GeneralPurposeLinuxAgentsCI
  condition: true
  steps:
  - template: /tech_products/azr4/templates/steps/add_tag.yaml
    parameters:
      type: 'build'
  - task: Bash@3
    inputs:
      targetType: inline
      script: |
        echo "##[debug] Nothing to do here..."
        export