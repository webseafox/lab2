steps:  
  - task: Bash@3
    name: load_variables
    displayName: 'Load Multibranch Variables'
    continueOnError: false
    inputs:
      targetType: inline
      script: |
        echo "##vso[task.setvariable variable=VERSION;isOutput=true]${VERSION}"