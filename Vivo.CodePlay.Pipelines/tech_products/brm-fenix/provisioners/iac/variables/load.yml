parameters:
  - name: config
    type: object
  - name: iacParameters
    type: object

variables:
  - group: BRM_IAC_TOGGLE_FEATURES

  - template: variables.yml
    parameters:
      config: ${{ parameters.config }}
      iacParameters: ${{ parameters.iacParameters }}