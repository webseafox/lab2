parameters:
  - name: gitTag
    type: string
    default: ""

steps:
- template: /tech_products/ecmc/core-pipelines/tasks/git/tag.yml
  parameters:
    staticTagName: ${{ parameters.gitTag }}
    condition: ne(variables.BUMP_VERSION_ENABLED, true)