parameters:
- name: pythonVersion
  type: string

steps:
- template: /tech_products/ecmc/core-pipelines/tasks/python/lint_with_make_and_docker.yml
  parameters:
    pythonVersion: ${{ parameters.pythonVersion }}

- template: /tech_products/ecmc/core-pipelines/tasks/python/tests_with_make_and_docker.yml
  parameters:
    pythonVersion: ${{ parameters.pythonVersion }}

- template: /tech_products/ecmc/core-pipelines/tasks/artifacts/publish_app.yml
  parameters:
    contents: |
      **/*
      !.git/**/*
      !?/**/*
      !**/*.log
      !.venv/**/*
