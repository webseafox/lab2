parameters:
- name: serviceConnection

steps:
- task: Kubernetes@1
  displayName: 'kubectl login (kubernetes)'
  inputs:
    kubernetesServiceEndpoint: ${{ parameters.serviceConnection }}
    command: login
    outputFormat: none