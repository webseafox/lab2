parameters:
- name: dependsOn
  type: object
  default: [project_validations]

stages:
- stage: build
  dependsOn: ${{ parameters.dependsOn }}
  displayName: Build, Test & Coverage
  pool: $(PRIVATE_AGENT_DEFAULT)
  jobs:
    - job: build
      displayName: Build
      timeoutInMinutes: 120
      steps:
        - template: /tech_products/ecmc/adbb-aem/provisioners/aem/b2b/default/templates/setup-maven.yml
        - template: /tech_products/ecmc/adbb-aem/provisioners/aem/b2b/default/templates/build.yml
        - template: /tech_products/ecmc/core-pipelines/tasks/artifacts/publish_app.yml
        - template: /tech_products/ecmc/core-pipelines/task_groups/tests/publish_tests_reports.yml
          parameters:
            workdirArtifacts: "$(Build.SourcesDirectory)/$(AEM_JAVA_BUNDLE_FOLDER_NAME)"
            pathToSources: "$(COVERAGE_REPORT_PATH_TO_SOURCE)"
            coverageFilePattern: "$(COVERAGE_REPORT_SUMMARY_FILE_LOCATION)"
            additionalCodeCoverageFiles: "$(COVERAGE_REPORT_ADDITIONAL_FILE)"
