parameters:
- name: enable_devflow_validations
  type: boolean
  default: False
- name: project_name
  type: string

variables:
- template: /tech_products/ecmc/sap-commerce/provisioners/variables/load.yaml

resources:
  repositories:
    - repository: FortifyExclusion

      name: DevOps/Vivo.Fortify.Exclusions
      type: git
      ref: refs/heads/master
      endpoint: CorePipelines

    - repository: CorePipelines
      name: DevOps/Vivo.Core.Pipelines
      type: git
      ref: refs/tags/v1
      endpoint: CorePipelines

    - repository: CorePipelinesScripts
      name: DevOps/Vivo.Core.Pipelines.Scripts
      type: git
      ref: refs/heads/master
      endpoint: CorePipelines

stages:

  - template: stages/init.yaml

  ## VALIDATE PREDEFINED STANDARDS (COMMIT MESSAGES, BRANCH NAME ETC)
  - ${{ if or(and(ne(variables['Build.Reason'],'PullRequest'), in(variables['Build.SourceBranch'], 'refs/heads/main', 'refs/heads/master')), startsWith(variables['Build.SourceBranch'], 'refs/heads/')) }}:
    - ${{ if eq(parameters.enable_devflow_validations, True) }}:
      - template: stages/validate_standards_dev_flow.yaml

    - template: stages/build_test.yaml
      parameters:
        enable_devflow_validations: ${{ parameters.enable_devflow_validations }}

    ## QUALITY GATE
    # - template: stages/gates.yaml
    - template: /provisioners/app/trunkbased/java/api_default/stages/gates.yml@CorePipelines

    # SECURITY
    - template: stages/security.yaml
      parameters:
        fortifyExclusionRepository: FortifyExclusion

    - ${{ if and(ne(variables['Build.Reason'],'PullRequest'), in(variables['Build.SourceBranch'], 'refs/heads/main', 'refs/heads/master'), in(variables['Build.Reason'], 'IndividualCI', 'BatchedCI')) }}:
      - template: stages/finalize.yaml


