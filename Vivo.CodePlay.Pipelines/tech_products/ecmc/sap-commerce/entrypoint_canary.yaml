parameters:
- name: blue_green
  type: string
  default: 'Não'
  values:
  - 'Não'
  - 'Sim'
- name: build_name
  type: string
  default: ''
- name: database_update_mode
  type: string
  default: 'NONE'
  values:
  - NONE
  - UPDATE
- name: deploy_strategy
  type: string
  default: 'RECREATE'
  values:
  - RECREATE
  - ROLLING_UPDATE
- name: environment_for_deploy
  type: string
  values:
  - p1
- name: is_rollback
  type: boolean
  default: false

# Template selection switches based on pipeline configuration parameters
extends:
  template: /tech_products/ecmc/sap-commerce/provisioners/gitflow/src-sap-commerce-cloud/provisioner_entry_canary.yaml
  parameters:
    build_name: ${{ parameters.build_name}}
    blue_green: ${{ parameters.blue_green }}
    database_update_mode: ${{ parameters.database_update_mode}}
    deploy_strategy: ${{ parameters.deploy_strategy }}
    environment_for_deploy: ${{ parameters.environment_for_deploy }}
    is_rollback: ${{ parameters.is_rollback }}
