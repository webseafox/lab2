

trigger: none

#Incluir Variaveis da Library
variables:
- name: ASDF_JDK_11
  value: "openjdk-11.0.2"
- name: System.debug
  value: "true"

stages:
- stage: deploy_helm_chart
  displayName: Deploy Helm Chart
  jobs:
  - job: deploy_helm_chart
    continueOnError: False
    condition: succeeded()
    displayName: Deploy Helm Chart
    pool: GeneralPurposeLinuxAgentsCI
    steps:

    - checkout: self
      fetchDepth: 1

    - task: AzureKeyVault@2  
      displayName: 'Getting AKV credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5  
      inputs:    
        azureSubscription: DevOpsSharedResources    
        KeyVaultName: kv-azdevops-shared
        SecretsFilter: >- 
          DOCKER-REGISTRY-USERNAME, DOCKER-REGISTRY-PASSWORD,
          HELM-REGISTRY-NEXUS-USERNAME, HELM-REGISTRY-NEXUS-PASSWORD,
          HELM-REGISTRY-ACR-USERNAME, HELM-REGISTRY-ACR-PASSWORD,
          NEXUS-DEPS-USR, NEXUS-DEPS-PSW
    
    - task: YamlParseFromVivo@4
      displayName: Get Helm/Kube Info
      name: deploy_helm_kube_info
      inputs:
        type: 'file'
        origin: $(Build.SourcesDirectory)/pipeline/properties.yaml
        jpaths: |
          var CHART_NAME = .helm.['chart-name']
          # "helm-conv86"
          var NEXUS_REPOSITORY = .helm.['nexus-repository']
          # "conv86-helm"
          var NEXUS_REPOSITORY_URL = .helm.['nexus-repository-url']
          # "http://10.129.178.173:8082/repository/conv86-helm"
          var KUBECTL_VERSION = .helm.['kubectl-version']
          # "v1.10.11"
          var HELM_VERSION = .helm.['helm-version']
          # "3.5.3"
          var RUN_LINT = .helm.['run-lint']
          # "false"

    - script: helm package .
      displayName: 'Helm Package'
      condition: succeeded()

    # - script: helm push ${CHART_ARCHIVE} $(NEXUS_REPOSITORY_URL)
    #   displayName: 'Helm push'
    #   condition: succeeded()
        
          