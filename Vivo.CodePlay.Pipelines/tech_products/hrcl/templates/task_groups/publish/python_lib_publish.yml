parameters:
- name: pythonVersion
  type: string

steps:
- template: /tech_products/hrcl/templates/tasks/python/twine_publish_to_artifacts.yml
  parameters:
    libPathLocation: Library/dist/*.whl
    downloadPath: $(Pipeline.Workspace)
    pythonVersion: ${{ parameters.pythonVersion }}
