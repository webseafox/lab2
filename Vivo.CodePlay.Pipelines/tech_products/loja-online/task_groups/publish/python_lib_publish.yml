parameters:
- name: pythonVersion
  type: string

steps:
- template: /tasks/python/twine_publish_to_artifacts.yml
  parameters:
    libPathLocation: Library/dist/*.whl
    downloadPath: $(Pipeline.Workspace)
    pythonVersion: ${{ parameters.pythonVersion }}
