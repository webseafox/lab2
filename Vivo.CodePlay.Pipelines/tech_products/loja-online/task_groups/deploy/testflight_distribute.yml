parameters:
- name: appIdentifier
- name: appSpecificId

steps:
- template: /tasks/flutter/deploy_testflight.yml
  parameters:
    appIdentifier: ${{ parameters.appIdentifier }}
    appSpecificId: ${{ parameters.appSpecificId }}