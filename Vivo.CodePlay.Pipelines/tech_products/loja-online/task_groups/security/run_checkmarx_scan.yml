parameters:
- name: skipSecurityGate
  default: false

steps:
- template: /tech_products/loja-online/tasks/checkmarx/prepare_gate.yml
  parameters:
    skipSecurityGate: ${{ parameters.skipSecurityGate }}

- template: /tech_products/loja-online/tasks/checkmarx/checkmarx_scan.yml
  parameters:
    additionalParams: $(CHECKMARX_THRESHOLD_ARGS)
    skipSecurityGate: ${{ parameters.skipSecurityGate }}
