jobs:
- job: prep
  pool: GeneralPurposeLinuxAgentsCI
  condition: true
  steps:
  - checkout: self
    persistCredentials: true
    clean: true
  - template: /tech_products/hdop/templates/steps/clone_submodules.yaml
    parameters:
      cloneGitSubmodules: ${{ variables['CLONE_GIT_SUBMODULES'] }}
      azureDevOpsPATToken: $(AZ_DEVOPS_PAT_TOKEN)
  - script: |
      export
      ls -la