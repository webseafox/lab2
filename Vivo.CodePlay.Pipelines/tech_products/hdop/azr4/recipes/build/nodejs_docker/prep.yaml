jobs:
- job: prep
  pool: GeneralPurposeLinuxAgentsCI
  condition: false
  steps:
  - template: /tech_products/hdop/templates/steps/version_update.yaml
  # - task: UseNode@1
  #   inputs:
  #     version: '18.x'
  # - task: AzureKeyVault@2
  #   displayName: 'Getting AKV Credentials'
  #   timeoutInMinutes: 1
  #   retryCountOnTaskFailure: 5
  #   inputs:
  #     azureSubscription: 'DevOpsSharedResources'
  #     KeyVaultName: 'kv-azdevops-shared'
  #     SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, NEXUS-DEFAULT-USR, NEXUS-DEFAULT-PSW'
  # - script: echo "##vso[task.setvariable variable=NEXUS_DEFAULT_USR]$(NEXUS-DEPS-USR)"
  #   displayName: Set NEXUS_DEFAULT_USR Var
  # - script: echo "##vso[task.setvariable variable=NEXUS_DEFAULT_PSW]$(NEXUS-DEPS-PSW)"
  #   displayName: Set NEXUS_DEFAULT_PSW Var
  # - bash: |
  #     echo "##[debug]DEFAULT_NPMRC_TEMP_FILE_PATH.....: ${DEFAULT_NPMRC_TEMP_FILE_PATH}"

  #     if [ -f .npmrc ]; then
  #       echo "##[debug]O arquivo .npmrc foi encontrado!"
  #     else
  #       echo "##[debug]O arquivo .npmrc não foi encontrado!"
  #       exit 0;
  #     fi

  #     echo ##vso[task.setvariable variable=NPM_CONFIG_USERCONFIG]${DEFAULT_NPMRC_TEMP_FILE_PATH}
  #   displayName: "Setting up .npmrc"
  #   env:
  #     DEFAULT_NPMRC_TEMP_FILE_PATH: $(DEFAULT_NPMRC_TEMP_FILE_PATH)
  # - bash: |
  #     if [ -f .npmrc ]; then
  #       auth_opt="email=vivo.devops.br@telefonica.com\nalways-auth=true"
  #       auth=$(echo -n ${NEXUS_DEFAULT_USR}:${NEXUS_DEFAULT_PSW} | openssl base64)
  #       repos=$(grep -o "registry=.*" .npmrc | cut -d':' -f2 | awk -v auth="$auth" '{print $0 ":_auth="auth""}')
  #       { printf '\n'; echo -e "$repos\n$auth_opt"; } | tee -a >(cat >> .npmrc) > /dev/null
  #       echo "##[debug] Credenciais configuradas!"
  #       echo "User Config: ${DEFAULT_NPMRC_TEMP_FILE_PATH}"
  #     else
  #       echo "##[debug]O arquivo .npmrc não foi encontrado!"
  #       exit 0;
  #     fi
  #   displayName: "Set dependencies credentials (Nexus)"
  #   env:
  #     NPM_CONFIG_USERCONFIG: $(DEFAULT_NPMRC_TEMP_FILE_PATH)