jobs:
- job: publish
  pool: GeneralPurposeLinuxAgentsCI
  workspace:
    clean: all
  condition: false
  steps:
  - task: Bash@3
    inputs:
      targetType: inline
      script: |
        echo "##[debug] Nothing to do here..."
        export