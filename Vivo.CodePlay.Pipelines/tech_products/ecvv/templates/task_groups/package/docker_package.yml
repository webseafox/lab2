parameters:
- name: artifactName
  default: app
- name: environment
  default: ""

steps:
- template: /tech_products/ecvv/templates/tasks/artifacts/download_app.yml
  parameters: 
    artifactName: ${{ parameters.artifactName }}
    
- template: /tech_products/ecvv/templates/tasks/docker/export_image_tags.yml
  parameters:
    namespace: $(GOV_APP_ACRONYM)
    appSystem: $(APP_SYSTEM)
    appName: $(APP_NAME)
    appVersion: $(APP_VERSION)
    environment: ${{ parameters.environment }}

- template: /tech_products/ecvv/templates/tasks/docker/build_publish_image.yml
  parameters:
    appName: $(APP_NAME)
    dockerImageName: $(DOCKER_IMAGE_NAME)
    dockerImageTag: $(DOCKER_IMAGE_TAG)
    appVersion: $(APP_VERSION)

- template: /tech_products/ecvv/templates/tasks/git/tag.yml
  parameters:
    staticTagName: "$(DOCKER_IMAGE_TAG)"
    condition: ne(variables.BUMP_VERSION_ENABLED, true)

- template: /tech_products/ecvv/templates/tasks/docker/export_metadata.yml

- template: /tech_products/ecvv/templates/tasks/artifacts/publish_docker.yml