parameters:
- name: chartNewVersion
  default: ""

steps:
- template: /tasks/helm/extract_chart_info.yml
- template: /tasks/helm/installer.yml
- template: /tasks/helm/package.yml
  parameters:
    chartNewVersion: ${{ parameters.chartNewVersion }}
- template: /tasks/helm/registry_login.yml
- template: /tasks/helm/publish_registry_acr.yml
  parameters:
      chartNewVersion: ${{ parameters.chartNewVersion }}
- template: /tasks/helm/publish_registry_nexus.yml
  parameters:
    chartNewVersion: ${{ parameters.chartNewVersion }}
- template: /tasks/telemetry/telemetry.yml