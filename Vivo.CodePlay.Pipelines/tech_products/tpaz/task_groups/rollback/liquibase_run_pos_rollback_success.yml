steps:
- template: /tasks/bash/welcome.yml
  parameters:
    message: $(DB_ROLLBACK_SUCCESS_MSG)

- template: /tasks/bash/add_azure_pipeline_tag.yml.yml
  parameters:
    displayName: 'Add Pipeline Tag'
    tagName: '$(System.StageName)-$(environment)=succeeded'
