steps:
- template: /tasks/artifacts/download_app.yml

- template: /tasks/nodejs/setup_credentials.yml
  parameters:
    artifactDir: "$(DEFAULT_WORKDIR_ARTIFACTS)"

- template: /tasks/nodejs/publish_to_npm.yml
  parameters:
    artifactDir: "$(DEFAULT_WORKDIR_ARTIFACTS)"
