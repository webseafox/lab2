parameters:
- name: app_acronym
  type: string
- name: oke
  type: string
- name: serviceconnection
  type: string
- name: pod
  type: string
- name: environment
  displayName: Environment
  type: string
  default: dev
  values:
  - dev
  - test
  - prod
- name: action
  displayName: Action
  type: string
  default: plan
  values:
  - plan
  - apply
  - destroy
  - planDestroy

- name: requiresManualApproval
  type: boolean
  default: 'false'

- name: isDestroy
  type: boolean
  default: 'false'

stages:
- stage: security_scan
  pool: GeneralPurposeLinuxAgentsCI
  displayName: Security Scan
  jobs:
  - job: AppSecConfigKeys
    displayName: 'Get AppSec Config Keys'
    steps:
    - template: /security/get_appconfig_keys_framework.yml
      parameters:
        azureSubscription: DevOpsSharedResources
        appConfigurationEndpoint: https://appcs-azdevops-shared.azconfig.io

  - job: run_fortify
    displayName: Run Fortify
    dependsOn: AppSecConfigKeys
    variables:
      USE_FORTIFY: $[ dependencies.AppSecConfigKeys.outputs['evaluate_flags.USE_FORTIFY'] ]
      USE_CONVISO: $[ dependencies.AppSecConfigKeys.outputs['evaluate_flags.USE_CONVISO'] ]
      SKIP_SECURITY_GATE: True
      FORTIFY_APP_VERSION: $[ dependencies.AppSecConfigKeys.outputs['scan_version_name.FORTIFY_APP_VERSION'] ]
    condition: and(succeeded(), eq(variables.USE_FORTIFY, 'true'))
    steps:
    - template: /security/define_appsec_app_version.yml

    - template: /security/run_fortify_scan.yml
      parameters:
        fortifyExclusionRepository: 'FortifyExclusion'
        enableFortifyExclusions: false
        appLanguage: 'java'
        appVersion: "$(FORTIFY_APP_VERSION)" # OBS: A variável FORTIFY_APP_VERSION é definida no template /security/define_appsec_app_version.yml
        convisoCompany: 1345
- stage: stage_plan
  pool:
    name: GeneralPurposeLinuxAgentsCD
  displayName: PLAN -> ${{parameters.environment}}
  dependsOn: 
  - security_scan
  condition: and(succeeded(), or(eq('${{parameters.action}}','plan'), eq('${{parameters.action}}','apply')))
  jobs:
  - job: job_plan
    displayName: PLAN -> ${{parameters.environment}}
    steps:
    - checkout: self
    - task: AzureKeyVault@2
      displayName: 'Obter chave privada do Key Vault'
      inputs:
        azureSubscription: kv-dvps-brsouth-shared
        KeyVaultName: kv-dvps-brsouth-shared
        SecretsFilter: 'svc-ocidevops-plat-code-${{ parameters.environment }}-private'

    - task: Bash@3
      displayName: About Stage
      inputs:
        targetType: inline
        script: |
            RED='\033[0;31m'
            GREEN='\033[0;32m'
            BLUE='\033[0;34m'
            YELLOW='\033[1;33m'
            BG_GREEN='\033[42;1;37m'

            echo ' ================================================================================================================================'
            echo '|                       __   __  _                  ___            _         ___   _                                             |'
            echo '|                       \ \ / / (_) __ __  ___     / __|  ___   __| |  ___  | _ \ | |  __ _   _  _                               |'
            echo '|                        \ V /  | | \ V / / _ \   | (__  / _ \ / _` | / -_) |  _/ | | / _` | | || |                              |'                 
            echo '|                         \_/   |_|  \_/  \___/    \___| \___/ \__,_| \___| |_|   |_| \__,_|  \_, |                              |'               
            echo '|                                                                                             |__/                               |'
            echo ' ================================================================================================================================'
            echo "                                                                                                      powered by Vivo DevOps Team"
            echo ""

            printf "
            =================================================================================================================================
            ${GREEN}                                                 ${SYSTEM_STAGEDISPLAYNAME} 
            =================================================================================================================================

              Lembre-se que a esteira apenas reflete o comportamento da sua aplicação e de acordo com as variáveis definidas.

            ${YELLOW} Em caso de dúvidas sobre essa fase do pipeline acesse a documentação: ${BLUE}https://wikicorp.telefonica.com.br/display/AC/Terraform+-+Azure

            =================================================================================================================================
            "
    - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
      displayName: 'Install Terraform 1.4.5'
      inputs:
        terraformVersion: 1.4.5
    - task: AzureKeyVault@2
      displayName: 'Getting AKV credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: DevOpsSharedResources
        KeyVaultName: kv-azdevops-shared
        SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, AZ-DEVOPS-PAT-TOKEN, PHPIPAM-PASSWORD, SVC-OCIALERTAS-PASSWORD'

    - task: CmdLine@2
      displayName: 'Setup git config https'
      env:
        GIT_PASS: $(AZ-DEVOPS-PAT-TOKEN)
        GIT_ASKPASS: "$(Agent.TempDirectory)/git_env_password.sh"
      inputs:
        script: |
          echo "##[debug] GIT_ASKPASS........: ${GIT_ASKPASS}"
          echo "exec echo \"$GIT_PASS\"" >> "${GIT_ASKPASS}"
          chmod 777 "${GIT_ASKPASS}"
          echo "##vso[task.setvariable variable=GIT_ASKPASS]$GIT_ASKPASS"
        workingDirectory: $(Agent.TempDirectory)
    - task: ms-devlabs.custom-terraform-tasks.custom-terraform-release-task.TerraformTaskV4@4
      displayName: 'Terraform INIT - OCI'
      inputs:
        backendServiceArm: SharedResources
        backendAzureRmResourceGroupName: rg-devops-sharedservices
        backendAzureRmStorageAccountName: sttfstatedevopsshared
        backendAzureRmContainerName: oci-terraform-state-${{parameters.environment}}
        backendAzureRmKey: $(System.TeamProject)/$(Build.Repository.Name)/terraform.tfstate
        workingDirectory: $(Build.SourcesDirectory)
      env:
        TF_LOG: ERROR

    - task: TerraformTaskV4@4
      displayName: 'Terraform VALIDATE'
      inputs:
        provider: 'oci'
        environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
        command: validate
        commandOptions: ''
        workingDirectory: $(Build.SourcesDirectory)
    - task: TerraformTaskV4@4
      displayName: 'Terraform PLAN'
      inputs:
        provider: 'oci'
        command: plan
        commandOptions: --out=tfplan.out -var-file=environments/${{ parameters.environment }}/terraform.tfvars.json
        environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
        environmentServiceNameAzureRM: mi-terraform-${{parameters.environment}}
        workingDirectory: $(Build.SourcesDirectory)
      env:
        AZDO_ORG_SERVICE_URL: https://dev.azure.com/telefonica-vivo-brasil
        KUBECONFIG: $(KUBECONFIG)
        TF_VAR_kubeconfig_path: $(KUBECONFIG)
        PHPIPAM_APP_ID: platformcode
        PHPIPAM_ENDPOINT_ADDR: http://10.240.42.97:8000/api
        PHPIPAM_PASSWORD: $(PHPIPAM-PASSWORD)
        PHPIPAM_USER_NAME: platformcode
        TF_VAR_NEXUS_USERNAME: $(NEXUS-DEPS-USR)
        TF_VAR_NEXUS_PASSWORD: $(NEXUS-DEPS-PSW)
        TF_VAR_SVC_OCIALERTAS_PASSWORD: $(SVC-OCIALERTAS-PASSWORD)
        TF_VAR_runtime_tags: '{"trilha":"${{parameters.environment}}","repositorio":"$(Build.Repository.Uri)"}'
        TF_VAR_azuredevops_org_url: https://dev.azure.com/telefonica-vivo-brasil
        TF_VAR_azuredevops_pat_token: $(System.AccessToken)
        ASDF_HELM_VERSION: 3.11.0

    - task: CmdLine@2
      displayName: 'Terraform SHOW - Export to .json'
      inputs:
        script: |
          terraform --version -json
          echo "##[command] terraform show -json tfplan.out | jq '.' > tfplan.json"
          terraform show -json tfplan.out | jq '.' > tfplan.json
        workingDirectory: $(Build.SourcesDirectory)
    - task: AzureKeyVault@2
      displayName: 'Getting AKV credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: DevOpsSharedResources
        KeyVaultName: kv-azdevops-shared
        SecretsFilter: 'INFRACOST-API-KEY'
    - task: CmdLine@2
      name: step_infracost_breakdown
      displayName: Infracost breakdown
      env:
        INFRACOST_API_KEY: $(INFRACOST-API-KEY)
      inputs:
        script: |
          infracost configure set currency BRL
          infracost breakdown --show-skipped --project-name $BUILD_REPOSITORY_NAME --path tfplan.json
        workingDirectory: $(Build.SourcesDirectory)
    - task: CmdLine@2
      name: step_checkov
      displayName: Run Checkov
      env:
        DIRECTORY: $(Build.SourcesDirectory)
        BASELINE: $(Build.SourcesDirectory)/.checkov.baseline
        VAR_FILE: environments/${{ parameters.environment }}/terraform.tfvars.json
        DOWNLOAD_EXTERNAL_MODULES: "true"
        OUTPUT_FILE_PATH: "console,$(System.DefaultWorkingDirectory)/TEST-Checkov.xml"
      inputs:
        script: |
          echo "##[debug] DIRECTORY....................: ${DIRECTORY}"
          echo "##[debug] BASELINE.....................: ${BASELINE}"
          echo "##[debug] VAR_FILE.....................: ${VAR_FILE}"
          echo "##[debug] DOWNLOAD_EXTERNAL_MODULES....: ${DOWNLOAD_EXTERNAL_MODULES}"
          echo "##[debug] OUTPUT_FILE_PATH.............: ${OUTPUT_FILE_PATH}"

          if [ ! -f "${BASELINE}" ]; then
          BASELINE="${AGENT_TEMPDIRECTORY}/.checkov.baseline"
          echo "##[debug] Arquivo .checkov.baseline não encontrado. Criando baseline padrão."
            cat <<EOF > "${BASELINE?}"
          {
            "failed_checks": []
          }
          EOF
          fi

          echo "##[command] checkov"
          checkov --directory ${DIRECTORY} \
            --baseline ${BASELINE} \
            --var-file ${VAR_FILE} \
            --download-external-modules ${DOWNLOAD_EXTERNAL_MODULES} \
            --skip-path tfplan.json \
            --skip-path examples \
            --output cli \
            --output junitxml \
            --output-baseline-as-skipped \
            --soft-fail \
            --output-file-path ${OUTPUT_FILE_PATH}
        workingDirectory: $(Build.SourcesDirectory)
    - task: CmdLine@2
      name: step_tfsec
      displayName: Run tfsec
      env:
        TFVARS_LOCATION: environments/${{ parameters.environment }}/terraform.tfvars.json
      inputs:
        script: |
          echo "##[command] tfsec --var-file ${TFVARS_LOCATION} --exclude-path examples --exclude-downloaded-modules --format junit --soft-fail --verbose --out ${SYSTEM_DEFAULTWORKINGDIRECTORY}/TEST-tfsec.xml"
          tfsec \
            --var-file ${TFVARS_LOCATION} \
            --exclude-path examples \
            --exclude-downloaded-modules \
            --format junit \
            --soft-fail \
            --verbose \
            --out ${SYSTEM_DEFAULTWORKINGDIRECTORY}/TEST-tfsec.xml
        workingDirectory: $(Build.SourcesDirectory)
    - task: PublishTestResults@2
      name: t_publish_test_result
      displayName: Publish Test Results
      inputs:
        mergeTestResults: false
        searchFolder: $(System.DefaultWorkingDirectory)
        testResultsFiles: '**/TEST-*.xml'
        testResultsFormat: JUnit
        testRunTitle: Test Results
        failTaskOnFailedTests: true
        publishRunAttachments: true
      continueOnError: true
      condition: succeeded()

    - script: |
        set -e
        echo "Verificando se há recursos para destroy"

        # Extrai os tipos de ações do plano Terraform
        CREATE_COUNT=$(jq '[.resource_changes[] | select(.change.actions[] == "create")] | length' tfplan.json)
        DESTROY_COUNT=$(jq '[.resource_changes[] | select(.change.actions[] == "delete")] | length' tfplan.json)

        echo "Recursos a adicionar: $CREATE_COUNT"
        echo "Recursos a destruir: $DESTROY_COUNT"

        # Se houver pelo menos um add e um destroy ao mesmo tempo, barramos a execução
        if [ "$CREATE_COUNT" -gt 0 ] && [ "$DESTROY_COUNT" -gt 0 ]; then
          echo "Aviso: O plano detectou que há recursos sendo destruídos e recriados ao mesmo tempo."
          echo "Definindo variável de controle para aprovação manual..."
          echo "##vso[task.setvariable variable=requiresManualApproval;isOutput=true]true"  # Define a variável de controle
        else
          echo "##vso[task.setvariable variable=requiresManualApproval;isOutput=true]false"
          echo "Não há conflitos de ADD e DESTROY."
        fi
      displayName: 'Verificando se há recursos para destroy'
      name: setRequiresApproval   
      workingDirectory: $(Build.SourcesDirectory)
      #condition: eq('${{ parameters.isDestroy }}', false)
      condition: and(eq('${{ parameters.isDestroy }}', false), eq('${{ parameters.action }}', 'apply'))

   # Aprovação manual somente se a variável requiresManualApproval for verdadeira
  - job: waitForValidation 
    displayName: Aprovação Manual para destroy
    pool: server
    timeoutInMinutes: 4320
    dependsOn: job_plan 
    #condition: and(succeeded(), eq('${{ parameters.isDestroy }}', false))
    condition: and(succeeded(), eq('${{ parameters.isDestroy }}', false), eq('${{ parameters.action }}', 'apply'))
    variables:
      requiresManualApproval: $[ dependencies.job_plan.outputs['setRequiresApproval.requiresManualApproval'] ]
    steps:
    - task: ManualValidation@1
      condition: eq(variables['requiresManualApproval'], 'true') 
      inputs:
        instructions: |
          O plano detectou recursos sendo destruídos e recriados ao mesmo tempo. 
          Por favor, aprove ou negue a execução dessa mudança.
          Caso não deseje permitir a destruição e recriação dos recursos, recuse a execução.
        onTimeout: 'reject'

- stage: stage_apply
  pool:
    name: GeneralPurposeLinuxAgentsCD
  displayName: APPLY -> ${{parameters.environment}}
  dependsOn:
  - stage_plan
  condition: and(succeeded(), eq('${{parameters.action}}','apply'))
  jobs:
  - deployment: job_apply
    displayName: APPLY -> ${{parameters.environment}}
    environment:
      name: terraform-approval-apply-${{parameters.environment}}
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
          - task: AzureKeyVault@2
            displayName: 'Obter chave privada do Key Vault'
            inputs:
              azureSubscription: kv-dvps-brsouth-shared
              KeyVaultName: kv-dvps-brsouth-shared
              SecretsFilter: 'svc-ocidevops-plat-code-${{ parameters.environment }}-private'
      


          - task: Bash@3
            displayName: About Stage
            inputs:
              targetType: inline
              script: |
                RED='\033[0;31m'
                GREEN='\033[0;32m'
                BLUE='\033[0;34m'
                YELLOW='\033[1;33m'
                BG_GREEN='\033[42;1;37m'

                echo ' ================================================================================================================================'
                echo '|                       __   __  _                  ___            _         ___   _                                             |'
                echo '|                       \ \ / / (_) __ __  ___     / __|  ___   __| |  ___  | _ \ | |  __ _   _  _                               |'
                echo '|                        \ V /  | | \ V / / _ \   | (__  / _ \ / _` | / -_) |  _/ | | / _` | | || |                              |'                 
                echo '|                         \_/   |_|  \_/  \___/    \___| \___/ \__,_| \___| |_|   |_| \__,_|  \_, |                              |'               
                echo '|                                                                                             |__/                               |'
                echo ' ================================================================================================================================'
                echo "                                                                                                      powered by Vivo DevOps Team"
                echo ""

                printf "
                =================================================================================================================================
                ${GREEN}                                                 ${SYSTEM_STAGEDISPLAYNAME} 
                =================================================================================================================================

                  Lembre-se que a esteira apenas reflete o comportamento da sua aplicação e de acordo com as variáveis definidas.

                ${YELLOW} Em caso de dúvidas sobre essa fase do pipeline acesse a documentação: ${BLUE}https://wikicorp.telefonica.com.br/display/AC/Terraform+-+Azure

                =================================================================================================================================
                "

          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
            displayName: 'Install Terraform 1.4.5'
            inputs:
              terraformVersion: 1.4.5 
          - task: AzureKeyVault@2
            displayName: 'Getting AKV credentials'
            timeoutInMinutes: 1
            retryCountOnTaskFailure: 5
            inputs:
              azureSubscription: DevOpsSharedResources
              KeyVaultName: kv-azdevops-shared
              SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, AZ-DEVOPS-PAT-TOKEN, PHPIPAM-PASSWORD, SVC-OCIALERTAS-PASSWORD'
          - task: CmdLine@2
            displayName: 'Setup git config https'
            env:
              GIT_PASS: $(AZ-DEVOPS-PAT-TOKEN)
              GIT_ASKPASS: "$(Agent.TempDirectory)/git_env_password.sh"
            inputs:
              script: |
                echo "##[debug] GIT_ASKPASS........: ${GIT_ASKPASS}"
                echo "exec echo \"$GIT_PASS\"" >> "${GIT_ASKPASS}"
                chmod 777 "${GIT_ASKPASS}"
                echo "##vso[task.setvariable variable=GIT_ASKPASS]$GIT_ASKPASS"
              workingDirectory: $(Agent.TempDirectory)  
          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-release-task.TerraformTaskV4@4
            displayName: 'Terraform INIT - azurerm'
            inputs:
              backendServiceArm: SharedResources
              backendAzureRmResourceGroupName: rg-devops-sharedservices
              backendAzureRmStorageAccountName: sttfstatedevopsshared
              backendAzureRmContainerName: oci-terraform-state-${{parameters.environment}}
              backendAzureRmKey: $(System.TeamProject)/$(Build.Repository.Name)/terraform.tfstate
              workingDirectory: $(Build.SourcesDirectory)
            env:
              TF_LOG: ERROR
          - task: TerraformTaskV4@4
            displayName: 'Terraform APPLY'
            inputs:
              provider: 'oci'
              command: apply
              commandOptions: -var-file=environments/${{ parameters.environment }}/terraform.tfvars.json --auto-approve
              environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
              environmentServiceNameAzureRM: sp-terraform-${{parameters.environment}}
              workingDirectory: $(Build.SourcesDirectory)
            env:
              AZDO_ORG_SERVICE_URL: https://dev.azure.com/telefonica-vivo-brasil
              KUBECONFIG: $(KUBECONFIG)
              TF_VAR_kubeconfig_path: $(KUBECONFIG)
              PHPIPAM_APP_ID: platformcode
              PHPIPAM_ENDPOINT_ADDR: http://10.240.42.97:8000/api
              PHPIPAM_PASSWORD: $(PHPIPAM-PASSWORD)
              PHPIPAM_USER_NAME: platformcode
              TF_VAR_NEXUS_USERNAME: $(NEXUS-DEPS-USR)
              TF_VAR_NEXUS_PASSWORD: $(NEXUS-DEPS-PSW)
              TF_VAR_SVC_OCIALERTAS_PASSWORD: $(SVC-OCIALERTAS-PASSWORD)
              TF_VAR_runtime_tags: '{"trilha":"${{parameters.environment}}","repositorio":"$(Build.Repository.Uri)"}'
              ASDF_HELM_VERSION: 3.11.0

          - ${{ if ne(parameters.serviceconnection, '') }}:
            - task: Kubernetes@1
              displayName: Login OKE ${{ parameters.oke }}-${{ parameters.environment }}
              inputs:
                kubernetesServiceEndpoint: ${{ parameters.serviceconnection }}-${{ parameters.environment }}
                command: login
                outputFormat: none


          - task: Bash@3
            displayName: "OCID Workload Identity DEV/PROD "
            condition: and(ne('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              BASE_NAMESPACE: ${{ parameters.app_acronym }}
              ENVIRONMENT: ${{ parameters.environment }}
              SERVICEACCOUNT_BASE: sa-${{ parameters.app_acronym }}
              POD_BASE: pod${{ parameters.pod }}
            inputs:
              targetType: inline
              script: |
                set -e
          
                NAMESPACE="${BASE_NAMESPACE}-${ENVIRONMENT}"
                SERVICEACCOUNT="${SERVICEACCOUNT_BASE}-${ENVIRONMENT}"
                POD="${POD_BASE}${ENVIRONMENT}"
          
                echo "▶️ Namespace: $NAMESPACE"
                echo "🔐 ServiceAccount: $SERVICEACCOUNT"
                echo "📦 Pod: $POD"

                kubectl run "$POD" -n "$NAMESPACE" --restart=Never --image=ignore --overrides='{
                  "apiVersion": "v1",
                  "spec": {
                    "serviceAccountName": "'"$SERVICEACCOUNT"'",
                    "automountServiceAccountToken": true,
                    "imagePullSecrets": [
                     { "name": "vcr-docker" }
                    ],
                    "volumes": [
                      { "name": "oci-config", "emptyDir": {} },
                      { "name": "tmp", "emptyDir": {} }
                    ],
                    "containers": [{
                      "name": "app",
                      "image": "acrsharedservices01.azurecr.io/external/ghcr/oracle/oci-cli:20260128",
                      "command": ["sleep","3600"],
                
                      "livenessProbe": {
                        "exec": { "command": ["echo", "ok"] },
                        "initialDelaySeconds": 10,
                        "periodSeconds": 30
                      },
                      "readinessProbe": {
                        "exec": { "command": ["echo", "ok"] },
                        "initialDelaySeconds": 5,
                        "periodSeconds": 10
                      },
                
                      "securityContext": {
                        "runAsNonRoot": true,
                        "runAsUser": 1000,
                        "allowPrivilegeEscalation": false,
                        "readOnlyRootFilesystem": true,
                        "capabilities": { "drop": ["ALL"] }
                      },
                
                      "volumeMounts": [
                        { "name": "oci-config", "mountPath": "/oracle/.oci" },
                        { "name": "tmp", "mountPath": "/tmp" }
                      ],
                
                      "resources": {
                        "requests": {
                          "cpu": "250m",
                          "memory": "256Mi",
                          "ephemeral-storage": "256Mi"
                        },
                        "limits": {
                          "cpu": "500m",
                          "memory": "512Mi",
                          "ephemeral-storage": "512Mi"
                        }
                      }
                    }]
                  }
                }'

                kubectl wait --for=condition=Ready pod/"$POD" -n "$NAMESPACE" --timeout=120s
          
                kubectl exec "$POD" -n "$NAMESPACE" -- \
                  oci iam db-token get --auth oke_workload_identity --region sa-saopaulo-1
          
                SUBJECT=$(kubectl exec "$POD" -n "$NAMESPACE" -- \
                  cat /oracle/.oci/db-token/token | cut -d '.' -f2 | base64 -d 2>/dev/null | jq -r .sub)
          
                echo "##vso[task.setvariable variable=adb_iam_subject;issecret=true]$SUBJECT"
          
                kubectl delete pod "$POD" -n "$NAMESPACE" --ignore-not-found
 
 

          - task: Bash@3
            displayName: "OCID Workload Identity Esteiras"
            condition: and(eq('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              BASE_NAMESPACE: ${{ parameters.app_acronym }}
              ENVIRONMENT: test
              SERVICEACCOUNT_BASE: sa-${{ parameters.app_acronym }}
              POD_BASE: pod${{ parameters.pod }}
            inputs:
              targetType: inline
              script: |
                set -e
          
                NAMESPACES=(
                  "${BASE_NAMESPACE}-esteira1"
                  "${BASE_NAMESPACE}-esteira2"
                  "${BASE_NAMESPACE}-preprod"
                  "${BASE_NAMESPACE}-prodlike"
                )
          
                for NAMESPACE in "${NAMESPACES[@]}"; do
                  ESTEIRA=$(echo "$NAMESPACE" | awk -F'-' '{print $NF}')
          
                  SERVICEACCOUNT="${SERVICEACCOUNT_BASE}-${ESTEIRA}"
                  POD="${POD_BASE}-${ESTEIRA}"
          
                  echo "---------------------------------------"
                  echo "▶️ Namespace: $NAMESPACE"
                  echo "🏷 Esteira: $ESTEIRA"
                  echo "👤 ServiceAccount: $SERVICEACCOUNT"
                  kubectl run "$POD" -n "$NAMESPACE" --restart=Never --image=ignore --overrides='{
                    "apiVersion": "v1",
                    "spec": {
                      "serviceAccountName": "'"$SERVICEACCOUNT"'",
                      "imagePullSecrets": [
                     { "name": "vcr-docker" }
                      ],
                      "automountServiceAccountToken": true,
                      "volumes": [
                        { "name": "oci-config", "emptyDir": {} },
                        { "name": "tmp", "emptyDir": {} }
                      ],
                      "containers": [{
                        "name": "app",
                        "image": "acrsharedservices01.azurecr.io/external/ghcr/oracle/oci-cli:20260128",
                        "command": ["sleep","3600"],
                  
                        "livenessProbe": {
                          "exec": { "command": ["echo", "ok"] },
                          "initialDelaySeconds": 10,
                          "periodSeconds": 30
                        },
                        "readinessProbe": {
                          "exec": { "command": ["echo", "ok"] },
                          "initialDelaySeconds": 5,
                          "periodSeconds": 10
                        },
                  
                        "securityContext": {
                          "runAsNonRoot": true,
                          "runAsUser": 1000,
                          "allowPrivilegeEscalation": false,
                          "readOnlyRootFilesystem": true,
                          "capabilities": { "drop": ["ALL"] }
                        },
                  
                        "volumeMounts": [
                          { "name": "oci-config", "mountPath": "/oracle/.oci" },
                          { "name": "tmp", "mountPath": "/tmp" }
                        ],
                  
                        "resources": {
                          "requests": {
                            "cpu": "250m",
                            "memory": "256Mi",
                            "ephemeral-storage": "256Mi"
                          },
                          "limits": {
                            "cpu": "500m",
                            "memory": "512Mi",
                            "ephemeral-storage": "512Mi"
                          }
                        }
                      }]
                    }
                  }'
             
                  kubectl wait --for=condition=Ready pod/"$POD" -n "$NAMESPACE" --timeout=120s
          
                  kubectl exec "$POD" -n "$NAMESPACE" -- \
                    oci iam db-token get --auth oke_workload_identity --region sa-saopaulo-1
          
                  SUBJECT=$(kubectl exec "$POD" -n "$NAMESPACE" -- \
                    cat /oracle/.oci/db-token/token | cut -d '.' -f2 | base64 -d 2>/dev/null | jq -r .sub)
          
                  echo "🔐 OCID capturado: $SUBJECT"
                  echo "##vso[task.setvariable variable=adb_iam_subject_${ESTEIRA};issecret=true]$SUBJECT"
          
                  kubectl delete pod "$POD" -n "$NAMESPACE" --ignore-not-found
                done
          
                echo "✅ Tokens de todas as esteiras gerados com sucesso"

          - task: Bash@3
            displayName: 'Exportar outputs do Terraform como variáveis'
            inputs:
              targetType: inline
              script: |
                set -e
                ADB_EZCONNECT=$(terraform output -raw adb_private_high_service)
          
                ADB_IP=$(terraform output -raw adb_private_ip)
                ADB_PORT=1522
                ADB_SERVICE=$(echo "$ADB_EZCONNECT" | sed -E 's|.*/(.*)|\1|')
                ADB_ADMIN_PASS=$(terraform output -raw adb_admin_password)
          
                echo "ADB_IP=$ADB_IP"
                echo "ADB_PORT=$ADB_PORT"
                echo "ADB_SERVICE=$ADB_SERVICE"
          
                echo "##vso[task.setvariable variable=adb_ip]$ADB_IP"
                echo "##vso[task.setvariable variable=adb_port]$ADB_PORT"
                echo "##vso[task.setvariable variable=adb_service]$ADB_SERVICE"
                echo "##vso[task.setvariable variable=adb_admin_pass;issecret=true]$ADB_ADMIN_PASS"
                echo "✅ Variáveis exportadas com sucesso"

          
          - task: Bash@3
            displayName: 'Habilitar OCI_IAM no Autonomous DB'
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
            inputs:
              targetType: inline
              script: |
                set -e
                set +H

                echo "🚀 Habilitando OCI_IAM no Autonomous Database"
                echo "➡️ IP: $ADB_IP"
                echo "➡️ Port: $ADB_PORT"
                echo "➡️ Service: $ADB_SERVICE"

                sqlplus -S admin/"$ADB_ADMIN_PASS"@"
                  (description=
                    (retry_count=20)
                    (retry_delay=3)
                    (address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))
                    (connect_data=(service_name=$ADB_SERVICE))
                    (security=(ssl_server_dn_match=no)))"<<EOF
                SET SERVEROUTPUT ON;
                WHENEVER SQLERROR EXIT SQL.SQLCODE;

                BEGIN
                  DBMS_CLOUD_ADMIN.ENABLE_EXTERNAL_AUTHENTICATION(type => 'OCI_IAM');
                  DBMS_OUTPUT.PUT_LINE('[OK] HABILITADO: OCI_IAM');
                EXCEPTION
                  WHEN OTHERS THEN
                    IF INSTR(LOWER(SQLERRM), 'already') > 0 THEN
                      DBMS_OUTPUT.PUT_LINE('[OK] JA ESTAVA ATIVO');
                    ELSE
                      DBMS_OUTPUT.PUT_LINE('[ERRO] ' || SQLERRM);
                      RAISE;
                    END IF;
                END;
                /
                EXIT;
                EOF

                echo "✅ OCI_IAM configurado com sucesso"
                    
          - task: Bash@3
            displayName: 'Create DEV/PRD User Database'
            condition: and(ne('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              IAM_SUBJECT: $(adb_iam_subject)
              APP_ACRONYM: ${{ parameters.app_acronym }}
              ENVIRONMENT: ${{ parameters.environment }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H
                SERVICEACCOUNT="sa-${APP_ACRONYM}-${ENVIRONMENT}"
          
                RAW_NAME="$SERVICEACCOUNT"
          
                DB_USER=$(echo "$RAW_NAME" \
                  | tr '[:lower:]' '[:upper:]' \
                  | sed 's/[^A-Z0-9_]/_/g' \
                  | sed 's/^[0-9]/U_&/' \
                  | sed 's/^/USER_ADB_/' \
                  | cut -c1-30)



          
                echo "🚀 Criando usuário IAM no Autonomous Database"
                echo "👤 Usuário Oracle gerado: $DB_USER"
          
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"<<EOF
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
                
                DECLARE
                  v_count INTEGER;
                BEGIN
                  SELECT COUNT(*) INTO v_count FROM dba_users WHERE username = UPPER('$DB_USER');
                
                  IF v_count = 0 THEN
                    EXECUTE IMMEDIATE q'[
                      CREATE USER $DB_USER
                      IDENTIFIED GLOBALLY AS 'IAM_PRINCIPAL_OCID=$IAM_SUBJECT'
                    ]';
                
                    EXECUTE IMMEDIATE 'GRANT DWROLE TO $DB_USER';
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO $DB_USER';
                  END IF;
                END;
                /
                EXIT;
                EOF
          
                echo "✅ Usuário IAM configurado com sucesso"

          - task: Bash@3
            displayName: 'Create Esteira1 User Database'
            condition: and(eq('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              IAM_SUBJECT: $(adb_iam_subject_esteira1)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H
                SERVICEACCOUNT="sa-${APP_ACRONYM}-esteira1"
                RAW_NAME="$SERVICEACCOUNT"

                DB_USER=$(echo "$RAW_NAME" \
                  | tr '[:lower:]' '[:upper:]' \
                  | sed 's/[^A-Z0-9_]/_/g' \
                  | sed 's/^[0-9]/U_&/' \
                  | sed 's/^/USER_ADB_/' \
                  | cut -c1-30)

                echo "=============================="
                echo "Criando usuário: $DB_USER"
                echo "IAM Subject: $IAM_SUBJECT"
                echo "=============================="
          
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"<<EOF
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
          
                DECLARE
                  v_count INTEGER;
                BEGIN
                  SELECT COUNT(*) INTO v_count FROM dba_users WHERE username = UPPER('$DB_USER');
          
                  IF v_count = 0 THEN
                    EXECUTE IMMEDIATE q'[CREATE USER $DB_USER IDENTIFIED GLOBALLY AS 'IAM_PRINCIPAL_OCID=$IAM_SUBJECT']';
                    EXECUTE IMMEDIATE 'GRANT DWROLE TO $DB_USER';
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO $DB_USER';
                  END IF;
                END;
                /
                EXIT;
                EOF

          - task: Bash@3
            displayName: 'Create Esteira2 User Database'
            condition: and(eq('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              IAM_SUBJECT: $(adb_iam_subject_esteira2)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H
          
                SERVICEACCOUNT="sa-${APP_ACRONYM}-esteira2"
                RAW_NAME="$SERVICEACCOUNT"

                DB_USER=$(echo "$RAW_NAME" \
                  | tr '[:lower:]' '[:upper:]' \
                  | sed 's/[^A-Z0-9_]/_/g' \
                  | sed 's/^[0-9]/U_&/' \
                  | sed 's/^/USER_ADB_/' \
                  | cut -c1-30)
                
                echo "=============================="
                echo "Criando usuário: $DB_USER"
                echo "IAM Subject: $IAM_SUBJECT"
                echo "=============================="
          
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"<<EOF
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
          
                DECLARE
                  v_count INTEGER;
                BEGIN
                  SELECT COUNT(*) INTO v_count FROM dba_users WHERE username = UPPER('$DB_USER');
          
                  IF v_count = 0 THEN
                    EXECUTE IMMEDIATE q'[CREATE USER $DB_USER IDENTIFIED GLOBALLY AS 'IAM_PRINCIPAL_OCID=$IAM_SUBJECT']';
                    EXECUTE IMMEDIATE 'GRANT DWROLE TO $DB_USER';
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO $DB_USER';
                  END IF;
                END;
                /
                EXIT;
                EOF

          - task: Bash@3
            displayName: 'Create PREPROD User Database'
            condition: and(eq('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              IAM_SUBJECT: $(adb_iam_subject_preprod)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H

                SERVICEACCOUNT="sa-${APP_ACRONYM}-preprod"
                RAW_NAME="$SERVICEACCOUNT"

                DB_USER=$(echo "$RAW_NAME" \
                  | tr '[:lower:]' '[:upper:]' \
                  | sed 's/[^A-Z0-9_]/_/g' \
                  | sed 's/^[0-9]/U_&/' \
                  | sed 's/^/USER_ADB_/' \
                  | cut -c1-30)
          
                echo "=============================="
                echo "Criando usuário: $DB_USER"
                echo "IAM Subject: $IAM_SUBJECT"
                echo "=============================="
                
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"<<EOF
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
          
                DECLARE
                  v_count INTEGER;
                BEGIN
                  SELECT COUNT(*) INTO v_count FROM dba_users WHERE username = UPPER('$DB_USER');
          
                  IF v_count = 0 THEN
                    EXECUTE IMMEDIATE q'[CREATE USER $DB_USER IDENTIFIED GLOBALLY AS 'IAM_PRINCIPAL_OCID=$IAM_SUBJECT']';
                    EXECUTE IMMEDIATE 'GRANT DWROLE TO $DB_USER';
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO $DB_USER';
                  END IF;
                END;
                /
                EXIT;
                EOF

          - task: Bash@3
            displayName: 'Create Prodlike User Database'
            condition: and(eq('${{ parameters.environment }}', 'test'), eq(variables['enable_workload_identity'], 'true'))
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              IAM_SUBJECT: $(adb_iam_subject_prodlike)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H
          
                SERVICEACCOUNT="sa-${APP_ACRONYM}-prodlike"
                RAW_NAME="$SERVICEACCOUNT"

                DB_USER=$(echo "$RAW_NAME" \
                  | tr '[:lower:]' '[:upper:]' \
                  | sed 's/[^A-Z0-9_]/_/g' \
                  | sed 's/^[0-9]/U_&/' \
                  | sed 's/^/USER_ADB_/' \
                  | cut -c1-30)

                echo "=============================="
                echo "Criando usuário: $DB_USER"
                echo "IAM Subject: $IAM_SUBJECT"
                echo "=============================="   
          
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"<<EOF
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
          
                DECLARE
                  v_count INTEGER;
                BEGIN
                  SELECT COUNT(*) INTO v_count FROM dba_users WHERE username = UPPER('$DB_USER');
          
                  IF v_count = 0 THEN
                    EXECUTE IMMEDIATE q'[CREATE USER $DB_USER IDENTIFIED GLOBALLY AS 'IAM_PRINCIPAL_OCID=$IAM_SUBJECT']';
                    EXECUTE IMMEDIATE 'GRANT DWROLE TO $DB_USER';
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO $DB_USER';
                  END IF;
                END;
                /
                EXIT;
                EOF

          - task: Bash@3
            displayName: 'Create IAM Group Users - DEV'
            condition: eq('${{ parameters.environment }}', 'dev')
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H
                
                ADB_CONN="(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"
                
                echo "🚀 Criando usuários de grupo IAM para DEV"
                
                sqlplus -S admin/"$ADB_ADMIN_PASS"@"$ADB_CONN" <<EOF
                SET SERVEROUTPUT ON;
                WHENEVER SQLERROR EXIT SQL.SQLCODE;
                DECLARE
                  PROCEDURE ensure_user(p_user VARCHAR2, p_role VARCHAR2) IS
                    v_user_exists   NUMBER;
                    v_grant_exists  NUMBER;
                  BEGIN
                    SELECT COUNT(*) INTO v_user_exists
                    FROM dba_users
                    WHERE username = UPPER(p_user);

                    IF v_user_exists = 0 THEN
                      EXECUTE IMMEDIATE 
                        'CREATE USER ' || p_user ||
                        ' IDENTIFIED GLOBALLY AS ''IAM_GROUP_NAME=Redecorp/' || p_user || '''';

                      DBMS_OUTPUT.PUT_LINE('[OK] USER CREATED: ' || p_user);
                    ELSE
                      DBMS_OUTPUT.PUT_LINE('[INFO] USER EXISTS: ' || p_user);
                    END IF;

                    SELECT COUNT(*) INTO v_grant_exists
                    FROM (
                      SELECT privilege AS name FROM dba_sys_privs WHERE grantee = p_user
                      UNION ALL
                      SELECT granted_role FROM dba_role_privs WHERE grantee = p_user
                    )
                    WHERE name = p_role;

                    IF v_grant_exists = 0 THEN
                      EXECUTE IMMEDIATE 'GRANT CONNECT TO ' || p_user;
                      EXECUTE IMMEDIATE 'GRANT ' || p_role || ' TO ' || p_user;
                      DBMS_OUTPUT.PUT_LINE('[OK] GRANTED ' || p_role || ' TO ' || p_user);
                    ELSE
                      DBMS_OUTPUT.PUT_LINE('[INFO] ALREADY GRANTED: ' || p_role || ' TO ' || p_user);
                    END IF;

                  END;

                BEGIN
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_READER', 'SELECT ANY TABLE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER', 'UNLIMITED TABLESPACE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'UNLIMITED TABLESPACE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'UNLIMITED TABLESPACE');

                  -- MAINTAINER SUPORTE
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'UNLIMITED TABLESPACE');


                END;
                /
                EXIT;
                EOF


                echo "✅ Todos os usuários de grupo do DEV criados com sucesso"

          - task: Bash@3
            displayName: 'Create IAM Group Users - TEST'
            condition: eq('${{ parameters.environment }}', 'test')
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H                 
                ADB_CONN="(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"

                echo "🚀 Criando usuários de grupo IAM para TEST"

                sqlplus -S admin/"$ADB_ADMIN_PASS"@"$ADB_CONN" <<EOF
                SET SERVEROUTPUT ON;
                WHENEVER SQLERROR EXIT SQL.SQLCODE;

                DECLARE
                  PROCEDURE ensure_user(p_user VARCHAR2, p_role VARCHAR2) IS
                    v_user_exists   NUMBER;
                    v_grant_exists  NUMBER;
                  BEGIN
                    -- CREATE USER (idempotente)
                    SELECT COUNT(*) INTO v_user_exists
                    FROM dba_users
                    WHERE username = UPPER(p_user);

                    IF v_user_exists = 0 THEN
                      EXECUTE IMMEDIATE 
                        'CREATE USER ' || UPPER(p_user) ||
                        ' IDENTIFIED GLOBALLY AS ''IAM_GROUP_NAME=Redecorp/' || UPPER(p_user) || '''';

                      DBMS_OUTPUT.PUT_LINE('[OK] USER CREATED: ' || UPPER(p_user));
                    ELSE
                      DBMS_OUTPUT.PUT_LINE('[INFO] USER EXISTS: ' || UPPER(p_user));
                    END IF;

                    SELECT COUNT(*) INTO v_grant_exists
                    FROM (
                      SELECT privilege AS name FROM dba_sys_privs WHERE grantee = UPPER(p_user)
                      UNION ALL
                      SELECT granted_role FROM dba_role_privs WHERE grantee = UPPER(p_user)
                    )
                    WHERE name = p_role;

                    IF v_grant_exists = 0 THEN
                      EXECUTE IMMEDIATE 'GRANT CONNECT TO ' || UPPER(p_user);
                      EXECUTE IMMEDIATE 'GRANT ' || p_role || ' TO ' || UPPER(p_user);
                      DBMS_OUTPUT.PUT_LINE('[OK] GRANTED ' || p_role || ' TO ' || UPPER(p_user));
                    ELSE
                      DBMS_OUTPUT.PUT_LINE('[INFO] ALREADY GRANTED: ' || p_role || ' TO ' || UPPER(p_user));
                    END IF;

                  END;

                BEGIN
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_READER', 'SELECT ANY TABLE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER', 'SELECT ANY TABLE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER', 'UNLIMITED TABLESPACE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_QA', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_QA', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_QA', 'UNLIMITED TABLESPACE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_DEVELOPER_SUPORTE', 'UNLIMITED TABLESPACE');

                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'DWROLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'SELECT ANY TABLE');
                  ensure_user('GOV_CORP_${APP_ACRONYM^^}_MAINTAINER_SUPORTE', 'UNLIMITED TABLESPACE');

                END;
                /
                EXIT;
                EOF

                echo "✅ Todos os usuários de grupo do TEST criados com sucesso"

          - task: Bash@3
            displayName: 'Create IAM Group Users - PROD'
            condition: eq('${{ parameters.environment }}', 'prod')
            env:
              ADB_ADMIN_PASS: $(adb_admin_pass)
              ADB_IP: $(adb_ip)
              ADB_PORT: $(adb_port)
              ADB_SERVICE: $(adb_service)
              APP_ACRONYM: ${{ parameters.app_acronym }}
            inputs:
              targetType: inline
              script: |
                set -e
                set +H

                ADB_CONN="(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(host=$ADB_IP)(port=$ADB_PORT))(connect_data=(service_name=$ADB_SERVICE))(security=(ssl_server_dn_match=no)))"

                echo "🚀 Criando usuários de grupo IAM para PROD"

                sqlplus -S admin/"$ADB_ADMIN_PASS"@"$ADB_CONN" <<EOF
                SET SERVEROUTPUT ON;
                WHENEVER SQLERROR EXIT SQL.SQLCODE;

                DECLARE
                  v_user_exists   NUMBER;
                  v_grant_exists  NUMBER;
                  v_user          VARCHAR2(200) := 'GOV_CORP_${APP_ACRONYM^^}_PCP';
                BEGIN
                  SELECT COUNT(*) INTO v_user_exists
                  FROM dba_users
                  WHERE username = v_user;

                  IF v_user_exists = 0 THEN
                    EXECUTE IMMEDIATE 
                      'CREATE USER ' || v_user ||
                      ' IDENTIFIED GLOBALLY AS ''IAM_GROUP_NAME=Redecorp/' || v_user || '''';

                    DBMS_OUTPUT.PUT_LINE('[OK] USER CREATED: ' || v_user);
                  ELSE
                    DBMS_OUTPUT.PUT_LINE('[INFO] USER EXISTS: ' || v_user);
                  END IF;

                  SELECT COUNT(*) INTO v_grant_exists
                  FROM dba_role_privs
                  WHERE grantee = v_user
                  AND granted_role = 'PDB_DBA';

                  IF v_grant_exists = 0 THEN
                    EXECUTE IMMEDIATE 'GRANT PDB_DBA TO ' || v_user;
                    EXECUTE IMMEDIATE 'GRANT UNLIMITED TABLESPACE TO ' || v_user;
                    DBMS_OUTPUT.PUT_LINE('[OK] GRANTED PDB_DBA TO ' || v_user);
                  ELSE
                    DBMS_OUTPUT.PUT_LINE('[INFO] PDB_DBA ALREADY GRANTED TO ' || v_user);
                  END IF;

                END;
                /
                EXIT;
                EOF

                echo "✅ Todos os usuários de grupo do PROD processados com sucesso"

          - task: CmdLine@2
            displayName: 'Terraform OUTPUT .json'
            env:
              OUTPUT_FILE: $(Build.ArtifactStagingDirectory)/outputs.json
            inputs:
              script: |
                terraform --version -json

                echo "##[command] terraform output -json > ${OUTPUT_FILE}"
                terraform output -json > ${OUTPUT_FILE}

                jq 'if . == {} then error("") else . end | {"outputs": "exported"}' ${OUTPUT_FILE}
                retVal=$?

                if [ $retVal -ne 0 ]; then
                  echo "##[debug] File '${OUTPUT_FILE}' is empty, removing it from artifact staging directory."
                  rm ${OUTPUT_FILE}
                fi
              workingDirectory: $(Build.SourcesDirectory)
          - task: PublishPipelineArtifact@1
            displayName: Publish Pipeline Artifact
            condition: ''
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)
              artifactName: terraform_apply
              publishLocation: pipeline

- stage: stage_plan_destroy
  pool:
    name: GeneralPurposeLinuxAgentsCD
  displayName: PLAN to DESTROY -> ${{parameters.environment}}
  dependsOn: []
  condition: and(succeeded(), or(eq('${{parameters.action}}','planDestroy'), eq('${{parameters.action}}','destroy')))
  jobs:
  - job: job_plan_destroy
    displayName: PLAN to DESTROY -> ${{parameters.environment}}
    steps:
    - checkout: self

    - task: AzureKeyVault@2
      displayName: 'Obter chave privada do Key Vault'
      inputs:
        azureSubscription: kv-dvps-brsouth-shared
        KeyVaultName: kv-dvps-brsouth-shared
        SecretsFilter: 'svc-ocidevops-plat-code-${{ parameters.environment }}-private'


    - task: Bash@3
      displayName: About Stage
      inputs:
        targetType: inline
        script: |
            RED='\033[0;31m'
            GREEN='\033[0;32m'
            BLUE='\033[0;34m'
            YELLOW='\033[1;33m'
            BG_GREEN='\033[42;1;37m'

            echo ' ================================================================================================================================'
            echo '|                       __   __  _                  ___            _         ___   _                                             |'
            echo '|                       \ \ / / (_) __ __  ___     / __|  ___   __| |  ___  | _ \ | |  __ _   _  _                               |'
            echo '|                        \ V /  | | \ V / / _ \   | (__  / _ \ / _` | / -_) |  _/ | | / _` | | || |                              |'                 
            echo '|                         \_/   |_|  \_/  \___/    \___| \___/ \__,_| \___| |_|   |_| \__,_|  \_, |                              |'               
            echo '|                                                                                             |__/                               |'
            echo ' ================================================================================================================================'
            echo "                                                                                                      powered by Vivo DevOps Team"
            echo ""

            printf "
            =================================================================================================================================
            ${GREEN}                                                 ${SYSTEM_STAGEDISPLAYNAME} 
            =================================================================================================================================

              Lembre-se que a esteira apenas reflete o comportamento da sua aplicação e de acordo com as variáveis definidas.

            ${YELLOW} Em caso de dúvidas sobre essa fase do pipeline acesse a documentação: ${BLUE}https://wikicorp.telefonica.com.br/display/AC/Terraform+-+Azure

            =================================================================================================================================
            "
    - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
      displayName: 'Install Terraform 1.4.5'
      inputs:
        terraformVersion: 1.4.5
    - task: AzureKeyVault@2
      displayName: 'Getting AKV credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: DevOpsSharedResources
        KeyVaultName: kv-azdevops-shared
        SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, AZ-DEVOPS-PAT-TOKEN, PHPIPAM-PASSWORD, SVC-OCIALERTAS-PASSWORD'

        
    - task: CmdLine@2
      displayName: 'Setup git config https'
      env:
        GIT_PASS: $(AZ-DEVOPS-PAT-TOKEN)
        GIT_ASKPASS: "$(Agent.TempDirectory)/git_env_password.sh"
      inputs:
        script: |
          echo "##[debug] GIT_ASKPASS........: ${GIT_ASKPASS}"
          echo "exec echo \"$GIT_PASS\"" >> "${GIT_ASKPASS}"
          chmod 777 "${GIT_ASKPASS}"
          echo "##vso[task.setvariable variable=GIT_ASKPASS]$GIT_ASKPASS"
        workingDirectory: $(Agent.TempDirectory)
    - task: ms-devlabs.custom-terraform-tasks.custom-terraform-release-task.TerraformTaskV4@4
      displayName: 'Terraform INIT - azurerm'
      inputs:
        backendServiceArm: SharedResources
        backendAzureRmResourceGroupName: rg-devops-sharedservices
        backendAzureRmStorageAccountName: sttfstatedevopsshared
        backendAzureRmContainerName: oci-terraform-state-${{parameters.environment}}
        backendAzureRmKey: $(System.TeamProject)/$(Build.Repository.Name)/terraform.tfstate
        workingDirectory: $(Build.SourcesDirectory)
      env:
        TF_LOG: ERROR
    - task: TerraformTaskV4@4
      displayName: 'Terraform VALIDATE'
      inputs:
        provider: 'oci'
        environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
        command: validate
        commandOptions: ''
        workingDirectory: $(Build.SourcesDirectory)
    - task: TerraformTaskV4@4
      displayName: 'Terraform PLAN - DESTROY'
      inputs:
        provider: 'oci'
        command: plan
        commandOptions: --out=tfplan.out -var-file=environments/${{ parameters.environment }}/terraform.tfvars.json -destroy
        environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
        environmentServiceNameAzureRM: mi-terraform-${{parameters.environment}}
        workingDirectory: $(Build.SourcesDirectory)
      env:
        AZDO_ORG_SERVICE_URL: https://dev.azure.com/telefonica-vivo-brasil
        TF_VAR_kubeconfig_path: $(KUBECONFIG)
        PHPIPAM_APP_ID: platformcode
        PHPIPAM_ENDPOINT_ADDR: http://10.240.42.97:8000/api
        PHPIPAM_PASSWORD: $(PHPIPAM-PASSWORD)
        PHPIPAM_USER_NAME: platformcode
        TF_VAR_NEXUS_USERNAME: $(NEXUS-DEPS-USR)
        TF_VAR_NEXUS_PASSWORD: $(NEXUS-DEPS-PSW)
        TF_VAR_SVC_OCIALERTAS_PASSWORD: $(SVC-OCIALERTAS-PASSWORD)
        TF_VAR_runtime_tags: '{"trilha":"${{parameters.environment}}","repositorio":"$(Build.Repository.Uri)"}'
        ASDF_HELM_VERSION: 3.11.0
        OCI_CONFIG_DIR: $(Build.SourcesDirectory)/$(Build.BuildId)/.oci
        OCI_CONFIG_FILE: $(Build.SourcesDirectory)/$(Build.BuildId)/.oci/config
        TF_VAR_azuredevops_org_url: https://dev.azure.com/telefonica-vivo-brasil

    - task: CmdLine@2
      displayName: 'Terraform SHOW - Export to .json'
      inputs:
        script: |
          terraform --version -json
          echo "##[command] terraform show -json tfplan.out | jq '.' > tfplan.json"
          terraform show -json tfplan.out | jq '.' > tfplan.json
        workingDirectory: $(Build.SourcesDirectory)
    - task: AzureKeyVault@2
      displayName: 'Getting AKV credentials for Infracost'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: DevOpsSharedResources
        KeyVaultName: kv-azdevops-shared
        SecretsFilter: 'INFRACOST-API-KEY'
    - task: CmdLine@2
      name: step_infracost_breakdown
      displayName: Infracost breakdown
      env:
        INFRACOST_API_KEY: $(INFRACOST-API-KEY)
      inputs:
        script: |
          infracost configure set currency BRL
          infracost breakdown --show-skipped --project-name $BUILD_REPOSITORY_NAME --path tfplan.json
        workingDirectory: $(Build.SourcesDirectory)
    - task: CmdLine@2
      name: step_checkov
      displayName: Run Checkov
      env:
        DIRECTORY: $(Build.SourcesDirectory)
        BASELINE: $(Build.SourcesDirectory)/.checkov.baseline
        VAR_FILE: environments/${{ parameters.environment }}/terraform.tfvars.json
        DOWNLOAD_EXTERNAL_MODULES: "true"
        OUTPUT_FILE_PATH: "console,$(System.DefaultWorkingDirectory)/TEST-Checkov.xml"
      inputs:
        script: |
          echo "##[debug] DIRECTORY....................: ${DIRECTORY}"
          echo "##[debug] BASELINE.....................: ${BASELINE}"
          echo "##[debug] VAR_FILE.....................: ${VAR_FILE}"
          echo "##[debug] DOWNLOAD_EXTERNAL_MODULES....: ${DOWNLOAD_EXTERNAL_MODULES}"
          echo "##[debug] OUTPUT_FILE_PATH.............: ${OUTPUT_FILE_PATH}"

          if [ ! -f "${BASELINE}" ]; then
          BASELINE="${AGENT_TEMPDIRECTORY}/.checkov.baseline"
          echo "##[debug] Arquivo .checkov.baseline não encontrado. Criando baseline padrão."
            cat <<EOF > "${BASELINE?}"
          {
            "failed_checks": []
          }
          EOF
          fi

          echo "##[command] checkov"
          checkov --directory ${DIRECTORY} \
            --baseline ${BASELINE} \
            --var-file ${VAR_FILE} \
            --download-external-modules ${DOWNLOAD_EXTERNAL_MODULES} \
            --skip-path tfplan.json \
            --skip-path examples \
            --output cli \
            --output junitxml \
            --output-baseline-as-skipped \
            --soft-fail \
            --output-file-path ${OUTPUT_FILE_PATH}
        workingDirectory: $(Build.SourcesDirectory)
    - task: CmdLine@2
      name: step_tfsec
      displayName: Run tfsec
      env:
        TFVARS_LOCATION: environments/${{ parameters.environment }}/terraform.tfvars.json
      inputs:
        script: |
          echo "##[command] tfsec --var-file ${TFVARS_LOCATION} --exclude-path examples --exclude-downloaded-modules --format junit --soft-fail --verbose --out ${SYSTEM_DEFAULTWORKINGDIRECTORY}/TEST-tfsec.xml"
          tfsec \
            --var-file ${TFVARS_LOCATION} \
            --exclude-path examples \
            --exclude-downloaded-modules \
            --format junit \
            --soft-fail \
            --verbose \
            --out ${SYSTEM_DEFAULTWORKINGDIRECTORY}/TEST-tfsec.xml
        workingDirectory: $(Build.SourcesDirectory)
    - task: PublishTestResults@2
      name: t_publish_test_result
      displayName: Publish Test Results
      inputs:
        mergeTestResults: false
        searchFolder: $(System.DefaultWorkingDirectory)
        testResultsFiles: '**/TEST-*.xml'
        testResultsFormat: JUnit
        testRunTitle: Test Results
        failTaskOnFailedTests: true
        publishRunAttachments: true
      continueOnError: true
      condition: succeeded()

- stage: stage_destroy
  pool:
    name: GeneralPurposeLinuxAgentsCD
  displayName: DESTROY -> ${{parameters.environment}}
  dependsOn:
  - stage_plan_destroy
  condition: and(succeeded(), eq('${{parameters.action}}','destroy'))
  jobs:
  - deployment: job_destroy
    displayName: DESTROY -> ${{parameters.environment}}
    environment:
      name: terraform-approval-destroy-${{parameters.environment}}
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self
          - task: AzureKeyVault@2
            displayName: 'Obter chave privada do Key Vault'
            inputs:
              azureSubscription: kv-dvps-brsouth-shared
              KeyVaultName: kv-dvps-brsouth-shared
              SecretsFilter: 'svc-ocidevops-plat-code-${{ parameters.environment }}-private'
      
          - task: Bash@3
            displayName: About Stage
            inputs:
              targetType: inline
              script: |
                RED='\033[0;31m'
                GREEN='\033[0;32m'
                BLUE='\033[0;34m'
                YELLOW='\033[1;33m'
                BG_GREEN='\033[42;1;37m'

                echo ' ================================================================================================================================'
                echo '|                       __   __  _                  ___            _         ___   _                                             |'
                echo '|                       \ \ / / (_) __ __  ___     / __|  ___   __| |  ___  | _ \ | |  __ _   _  _                               |'
                echo '|                        \ V /  | | \ V / / _ \   | (__  / _ \ / _` | / -_) |  _/ | | / _` | | || |                              |'                 
                echo '|                         \_/   |_|  \_/  \___/    \___| \___/ \__,_| \___| |_|   |_| \__,_|  \_, |                              |'               
                echo '|                                                                                             |__/                               |'
                echo ' ================================================================================================================================'
                echo "                                                                                                      powered by Vivo DevOps Team"
                echo ""

                printf "
                =================================================================================================================================
                ${GREEN}                                                 ${SYSTEM_STAGEDISPLAYNAME} 
                =================================================================================================================================

                  Lembre-se que a esteira apenas reflete o comportamento da sua aplicação e de acordo com as variáveis definidas.

                ${YELLOW} Em caso de dúvidas sobre essa fase do pipeline acesse a documentação: ${BLUE}https://wikicorp.telefonica.com.br/display/AC/Terraform+-+Azure

                =================================================================================================================================
                "

          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
            displayName: 'Install Terraform 1.4.5'
            inputs:
              terraformVersion: 1.4.5 
          - task: AzureKeyVault@2
            displayName: 'Getting AKV credentials'
            timeoutInMinutes: 1
            retryCountOnTaskFailure: 5
            inputs:
              azureSubscription: DevOpsSharedResources
              KeyVaultName: kv-azdevops-shared
              SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, AZ-DEVOPS-PAT-TOKEN, PHPIPAM-PASSWORD, SVC-OCIALERTAS-PASSWORD'
          - task: CmdLine@2
            displayName: 'Setup git config https'
            env:
              GIT_PASS: $(AZ-DEVOPS-PAT-TOKEN)
              GIT_ASKPASS: "$(Agent.TempDirectory)/git_env_password.sh"
            inputs:
              script: |
                echo "##[debug] GIT_ASKPASS........: ${GIT_ASKPASS}"
                echo "exec echo \"$GIT_PASS\"" >> "${GIT_ASKPASS}"
                chmod 777 "${GIT_ASKPASS}"
                echo "##vso[task.setvariable variable=GIT_ASKPASS]$GIT_ASKPASS"
              workingDirectory: $(Agent.TempDirectory)
          - task: ms-devlabs.custom-terraform-tasks.custom-terraform-release-task.TerraformTaskV4@4
            displayName: 'Terraform INIT - azurerm'
            inputs:
              backendServiceArm: SharedResources
              backendAzureRmResourceGroupName: rg-devops-sharedservices
              backendAzureRmStorageAccountName: sttfstatedevopsshared
              backendAzureRmContainerName: oci-terraform-state-${{parameters.environment}}
              backendAzureRmKey: $(System.TeamProject)/$(Build.Repository.Name)/terraform.tfstate
              workingDirectory: $(Build.SourcesDirectory)
            env:
              TF_LOG: $(TF_LOG)  
          - task: CmdLine@2
            displayName: 'Terraform OUTPUT .json'
            env:
              OUTPUT_FILE: $(Build.ArtifactStagingDirectory)/outputs.json
            inputs:
              script: |
                terraform --version -json

                echo "##[command] terraform output -json > ${OUTPUT_FILE}"
                terraform output -json > ${OUTPUT_FILE}

                jq 'if . == {} then error("") else . end | {"outputs": "exported"}' ${OUTPUT_FILE}
                retVal=$?

                if [ $retVal -ne 0 ]; then
                  echo "##[debug] File '${OUTPUT_FILE}' is empty, removing it from artifact staging directory."
                  rm ${OUTPUT_FILE}
                fi
              workingDirectory: $(Build.SourcesDirectory)
          - task: PublishPipelineArtifact@1
            displayName: Publish Pipeline Artifact
            condition: ''
            inputs:
              targetPath: $(Build.ArtifactStagingDirectory)
              artifactName: terraform_destroy
              publishLocation: pipeline
          - task: TerraformTaskV4@4
            displayName: 'Terraform DESTROY'
            inputs:
              provider: 'oci'
              command: destroy
              commandOptions: -var-file=environments/${{ parameters.environment }}/terraform.tfvars.json --auto-approve
              environmentServiceNameOCI: svc-ocidevops-plat-code-${{parameters.environment}}-terraform-gru
              environmentServiceNameAzureRM: sp-terraform-${{parameters.environment}}
              workingDirectory: $(Build.SourcesDirectory)
            env:
              AZDO_ORG_SERVICE_URL: https://dev.azure.com/telefonica-vivo-brasil
              PHPIPAM_APP_ID: platformcode
              PHPIPAM_ENDPOINT_ADDR: http://10.240.42.97:8000/api
              PHPIPAM_PASSWORD: $(PHPIPAM-PASSWORD)
              TF_VAR_kubeconfig_path: $(KUBECONFIG)
              PHPIPAM_USER_NAME: platformcode
              TF_VAR_NEXUS_USERNAME: $(NEXUS-DEPS-USR)
              TF_VAR_NEXUS_PASSWORD: $(NEXUS-DEPS-PSW)
              TF_VAR_SVC_OCIALERTAS_PASSWORD: $(SVC-OCIALERTAS-PASSWORD)
              KUBECONFIG: $(KUBECONFIG)
              TF_VAR_runtime_tags: '{"trilha":"${{parameters.environment}}","repositorio":"$(Build.Repository.Uri)"}'
              ASDF_HELM_VERSION: 3.11.0
