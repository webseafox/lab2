parameters:
- name: srcPath
- name: patchesPath

steps:
- script: |
    echo "Starting Build Phase"
  displayName: Starting Build Phase

- task: oc-cmd@3
  inputs:
    connectionType: 'OpenShift Connection Service'
    openshiftService: api-ocp-kafka-dev
    version: '$(OPENSHIFT_CLI_VERSION_URL)'
    proxy: '$(OPENSHIFT_PROXY)'
    cmd: "patch --filename ${{ parameters.srcPath }}/$(TOPIC_DEFAULT_FILE_NAME) --patch-file ${{ parameters.patchesPath }}/dev/strimzi-metadata.yml -n desenvolvimento --local=true --type merge --output yaml > ${{ parameters.srcPath }}/patched-topic.yaml"
  displayName: Patching Topic

- task: oc-cmd@3
  inputs:
    connectionType: 'OpenShift Connection Service'
    openshiftService: api-ocp-kafka-dev
    version: '$(OPENSHIFT_CLI_VERSION_URL)'
    proxy: '$(OPENSHIFT_PROXY)'
    cmd: 'apply --dry-run=server --filename ${{ parameters.srcPath }}/patched-topic.yaml -n desenvolvimento'
  displayName: Dry Running Topic

- task: oc-cmd@3
  inputs:
    connectionType: 'OpenShift Connection Service'
    openshiftService: api-ocp-kafka-dev
    version: '$(OPENSHIFT_CLI_VERSION_URL)'
    proxy: '$(OPENSHIFT_PROXY)'
    cmd: 'apply --filename ${{ parameters.srcPath }}/patched-topic.yaml -n desenvolvimento'
  condition: succeeded()
  displayName: Deploying Topic in Dev Environment
