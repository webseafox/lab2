parameters:
- name: updateVersion
  type: boolean
  default: false
- name: pythonVersion
  type: string

variables:
- template: /provisioners/app/global/python/variables/init.yml
  parameters:
    pythonVersion: ${{ parameters.pythonVersion }}

# Overrite Variables
- name: LINT_EXCLUDE_PATHS
  value: ""

- name: EXCLUDE_NOTEBOOK_DIR
  value: ""

- group: "azdo-team-project-variables"

- group: "${{ variables['Build.DefinitionName'] }}"

- template: /.azuredevops/variables/private.yml@self

# variables for databricks/notebooks_default
- name: CORPORATE_WIKI_DOC_URL
  value: "https://wikicorp.telefonica.com.br/display/AC/<TO-DO>"

- name: BUMP_VERSION_ENABLED
  value: $[and(eq(${{parameters.updateVersion}}, true), or(eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['Build.SourceBranch'], 'refs/heads/master')))]
