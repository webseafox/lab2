stages:
- stage: preparacao
  displayName: "Preparacao"
  jobs:
  - job: preparacao
    displayName: "Preparacao"
    pool:
      name: GeneralPurposeLinuxAgentsCI
    steps:
      - template: /tech_products/srus/tasks/artifacts/publish_catalog_info.yml
        parameters:
          breakWhenNotFound: true