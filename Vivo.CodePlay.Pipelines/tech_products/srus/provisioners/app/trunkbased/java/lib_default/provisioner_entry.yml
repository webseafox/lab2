parameters:
- name: bumpVersionOnManual
  type: boolean
  default: false

variables:
  - template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/variables/load.yml

resources:
  repositories:
    - repository: FortifyExclusion
      name: DevOps/Vivo.Fortify.Exclusions
      type: git
      ref: refs/heads/master
      endpoint: CorePipelines

stages:
## INIT
- template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/init.yml

## BUILD & TEST
- template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/build_test.yml
  parameters:
    bumpVersionOnManual: ${{ parameters.bumpVersionOnManual }}

## GATES
- template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/gates.yml

## SECURITY
- template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/security.yml
  parameters:
    fortifyExclusionRepository: FortifyExclusion

## VALIDATIONS
##- template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/validation.yml
  
## PUBLISH
- ${{ if and(not(startsWith(variables['Build.SourceBranch'], 'refs/tags/')), in(variables['Build.SourceBranch'], 'refs/heads/main', 'refs/heads/master')) }}:
  - template: /tech_products/srus/provisioners/app/trunkbased/java/lib_default/stages/publish.yml
    parameters:
      environment: $(DEPLOY_ENVIRONMENT_LIB)
      createGitTag: true