parameters:
- name: pythonVersion
  type: string

stages:
- stage: gate
  displayName: "Quality Gates"
  dependsOn: 
  - build_test
  pool: GeneralPurposeLinuxAgentsCI
  jobs:
  - job: sonarqube
    condition: and(succeeded(), ne(variables.skip_sonarqube, true), eq(variables.flowIsTagging, false))
    displayName: Code Analysis
    steps:
    - template: /tech_products/srus/tasks/bash/welcome.yml
    - ${{ if or(in(variables['Build.SourceBranch'], 'refs/heads/main', 'refs/heads/master', 'refs/heads/release'), startsWith(variables['Build.SourceBranch'], 'refs/pull/')) }}:
      - template: /tech_products/srus/tasks/catalog_info/extract_variables.yml
        parameters:
          workingDirectory: $(Build.SourcesDirectory)
          skipDownload: true
      - template: /tech_products/srus/task_groups/gates/run_sonarqube_scan.yml
        parameters:
          stack: "PYTHON"
          pythonVersion: "${{parameters.pythonVersion}}"
          projectKey: "$(SONAR_PROJECT_KEY)"