parameters:
- name: pythonVersion
  default: '3.9'
  values:
    - "3.7"
    - "3.9"
    - "3.10"
    - "3.11"

- name: updateVersion
  default: false
  type: boolean

- name: incrementLevel
  default: patch

- name: environment
  type: string

- name: diretorio
  type: string
  default: "SIRIUS_CORE"
  values:
    - "SIRIUS_CORE"
    - "SIRIUS_CORE_BSIM"

- name: unityCatalog
  type: boolean

variables:
- template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/variables/load.yml
  parameters:
    updateVersion: ${{ parameters.updateVersion }}
    pythonVersion: ${{ parameters.pythonVersion }}

resources:
  repositories:
    - repository: FortifyExclusion
      name: DevOps/Vivo.Fortify.Exclusions
      type: git
      ref: refs/heads/master
      endpoint: CorePipelines

stages:
# CI / CD Flow

## INIT
- template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/init.yml

- ${{ if or(eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['Build.SourceBranch'], 'refs/heads/master') ) }}:
  ## BUMP VERSION
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/bump_version.yml
    parameters:
      incrementLevel: ${{ parameters.incrementLevel }}

  ## BUILD & TEST
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/build_test.yml
    parameters:
      pythonVersion: ${{parameters.pythonVersion}}

  ## GATES
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/gates.yml
    parameters:
      pythonVersion: ${{parameters.pythonVersion}}

  ## SECURITY
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/security.yml
    parameters:
      fortifyExclusionRepository: FortifyExclusion

  ## PACKAGE
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/package.yml
    parameters:
      pythonVersion: ${{parameters.pythonVersion}}
      incrementLevel: ${{ parameters.incrementLevel }}
      unityCatalog: ${{ parameters.unityCatalog }}
      diretorio: ${{ parameters.diretorio }}
      environment: ${{ parameters.environment }}

  ## VALIDATIONS
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/validation.yml

  ## DEPLOY
  - ${{ if or(eq(variables['Build.SourceBranch'], 'refs/heads/main'), eq(variables['Build.SourceBranch'], 'refs/heads/master') ) }}:
    - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/deploy.yml
      parameters:
        environment: dev
        gitTag: true
        pythonVersion: ${{parameters.pythonVersion}}
        # incrementLevel: ${{ parameters.incrementLevel }}

# Manual Release Flow
- ${{ if startsWith(variables['Build.SourceBranch'], 'refs/tags/') }}:
  - template: /tech_products/srus/provisioners/app/trunkbased/python/databricks_default/stages/release.yml
    parameters:
      ${{ if eq(parameters.environment, 'dev') }}:
        environment: dev
        pythonVersion: ${{ parameters.pythonVersion }}
        databricksHost: $(BASE_DATABRICKS_URL_DEV)
        unityCatalog: ${{ parameters.unityCatalog }}
        diretorio: ${{ parameters.diretorio }}
        proxyAddl: $(PROXY_DEV)

      ${{ if eq(parameters.environment, 'homologation') }}:
        environment: homologation
        pythonVersion: ${{ parameters.pythonVersion }}
        databricksHost: $(BASE_DATABRICKS_URL_HML)
        unityCatalog: ${{ parameters.unityCatalog }}
        diretorio: ${{ parameters.diretorio }}
        proxyAddl: $(PROXY_HML)
        
      ${{ if eq(parameters.environment, 'bsim') }}:
        environment: bsim
        pythonVersion: ${{ parameters.pythonVersion }}
        databricksHost: $(BASE_DATABRICKS_URL_PRD)
        unityCatalog: ${{ parameters.unityCatalog }}
        diretorio: ${{ parameters.diretorio }}
        proxyAddl: $(PROXY_PRD)

      ${{ if eq(parameters.environment, 'production') }}:
        environment: production
        pythonVersion: ${{ parameters.pythonVersion }}
        databricksHost: $(BASE_DATABRICKS_URL_NPRD)
        unityCatalog: ${{ parameters.unityCatalog }}
        diretorio: ${{ parameters.diretorio }}
        proxyAddl: $(PROXY_NPRD)


