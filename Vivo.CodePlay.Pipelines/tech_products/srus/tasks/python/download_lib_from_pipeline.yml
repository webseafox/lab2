parameters:
- name: downloadPath
  default: $(DEFAULT_WORKDIR_ARTIFACTS)

steps:
- task: DownloadBuildArtifacts@1
  displayName: 'Download Artifact Python Library'
  inputs:
    artifactName: Library
    downloadPath: ${{ parameters.downloadPath }}