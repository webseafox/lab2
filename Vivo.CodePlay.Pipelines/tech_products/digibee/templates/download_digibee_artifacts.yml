parameters:
- name: downloadPath
  default: '$(System.ArtifactsDirectory)'

steps:
- task: DownloadBuildArtifacts@1
  displayName: 'Download Build digibee'
  inputs:
    artifactName: digibee
    parallelizationLimit: 20
    extractTars: false
    downloadPath: ${{ parameters.downloadPath }}