# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

stages:
- stage: build
  displayName: Build
  jobs:
  - job: package
    displayName: Build, Test and Pack
    pool: GeneralPurposeLinuxAgentsCI

    steps:
    - checkout: self
      fetchDepth: 1
      persistCredentials: true

    # # - script: asdf shim-versions node
    # - script: asdf local nodejs 14.21.3
    # - script: asdf current

    - task: NodeTool@0
      inputs:
        versionSource: 'spec'
        versionSpec: '21.x'
      env:
        HTTPS_PROXY: http://10.240.58.39:3128 # SQUID PROXY
        HTTP_PROXY: http://10.240.58.39:3128 # SQUID PROXY
        NO_PROXY: localhost,0.0.0.0,127.0.0.1,10.244.0.0/16,192.168.0.0/16,10.129.178.173,nexus.telefonica.com.br,acrsharedservices01.azurecr.io,appcs-azdevops-shared.azconfig.io,scm.azurewebsites.net

    - task: AzureKeyVault@2
      displayName: 'Getting AKV Credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: 'DevOpsSharedResources'
        KeyVaultName: 'kv-azdevops-shared'
        SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, NEXUS-DEFAULT-USR, NEXUS-DEFAULT-PSW'

    - script: echo "##vso[task.setvariable variable=NEXUS_USR]$(NEXUS-DEFAULT-USR)"
      displayName: Set NEXUS_USR Var

    - script: npm version patch --no-git-tag-version
      displayName: Bump version

    - script: npm i -ddd
      displayName: Install dependencies
    
    - script: npm run build-ci
      displayName: Build CI

    - script: npm pack
      displayName: Pack

    - task: JsonParseFromVivo@4
      displayName: Get package.json info
      name: package_info
      inputs:
        type: 'file'
        origin: $(Build.SourcesDirectory)/package.json
        jpaths: |
          var PACKAGE_JSON_VERSION =  .version

    - script: ls -lha $(build.sourcesdirectory)

    - task: CopyFiles@2
      name: t_copy_file
      displayName: 'Staging Artifacts'
      condition: succeeded()
      continueOnError: false
      inputs:
        SourceFolder: '$(build.sourcesdirectory)'
        TargetFolder: '$(Build.ArtifactStagingDirectory)'
        Contents: |
          *.tgz
          package.json
        CleanTargetFolder: true
        OverWrite: true
        flattenFolders: false

    - task: PublishBuildArtifacts@1
      displayName: 'Publish Artifacts'
      condition: succeeded()
      continueOnError: false
      inputs:
        ArtifactName: bin
        
    - task: Bash@3
      displayName: Commit new version
      name: commit_new_version
      continueOnError: false
      inputs:
        targetType: inline
        script: |-
          
          git stash save package.json
          git clean -fdX
          
          export TAG="${PACKAGE_JSON_VERSION:4}+$(date -u +"%Y%m%d%H%M")-${BUILD_SOURCEBRANCHNAME}-${BUILD_BUILDID}"

          echo "##[command] git checkout ${BUILD_SOURCEBRANCHNAME} --"
          git fetch
          git checkout ${BUILD_SOURCEBRANCHNAME} --
          git pull --rebase origin ${BUILD_SOURCEBRANCHNAME}
          git stash pop

          echo "##[command] git commit"
          git add package.json
          git commit -m "[skip ci] CodePlay bump version ${TAG}"

          echo "##[command]git tag ${TAG}"
          git tag ${TAG} -m "Version has been updated to ${TAG}"

          echo "##[command] git push origin ${BUILD_SOURCEBRANCHNAME}"
          git push origin --tags ${BUILD_SOURCEBRANCHNAME}

- stage: deploy
  displayName: Deploy
  jobs:
  - job: deploy_nexus
    displayName: Deploy on Nexus
    pool: GeneralPurposeLinuxAgentsCI

    steps:
    - checkout: none

    - task: DownloadBuildArtifacts@1
      displayName: Download artifacts
      inputs:
        buildType: 'current'
        downloadPath: '$(System.ArtifactsDirectory)'

    - task: NodeTool@0
      inputs:
        versionSource: 'spec'
        versionSpec: '21.x'
      env:
        HTTPS_PROXY: http://10.240.58.39:3128 # SQUID PROXY
        HTTP_PROXY: http://10.240.58.39:3128 # SQUID PROXY
        NO_PROXY: localhost,0.0.0.0,127.0.0.1,10.244.0.0/16,192.168.0.0/16,10.129.178.173,nexus.telefonica.com.br,acrsharedservices01.azurecr.io,appcs-azdevops-shared.azconfig.io,scm.azurewebsites.net
    
    - task: AzureKeyVault@2
      displayName: 'Getting AKV Credentials'
      timeoutInMinutes: 1
      retryCountOnTaskFailure: 5
      inputs:
        azureSubscription: 'DevOpsSharedResources'
        KeyVaultName: 'kv-azdevops-shared'
        SecretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW, NEXUS-DEFAULT-USR, NEXUS-DEFAULT-PSW'

#    - script: npm i -ddd
#      displayName: Dependencies
#      env:
#        HTTPS_PROXY: http://10.240.58.39:3128 # SQUID PROXY
#        HTTP_PROXY: http://10.240.58.39:3128 # SQUID PROXY
#        NO_PROXY: localhost,0.0.0.0,127.0.0.1,10.244.0.0/16,192.168.0.0/16,10.129.178.173,nexus.telefonica.com.br,acrsharedservices01.azurecr.io,appcs-azdevops-shared.azconfig.io,scm.azurewebsites.net

#    - script: npm init
#      displayName: npm init

#    - script: npm cache clean
#      displayName: npm cache clean

#    - script: npm run build-ci
#      displayName: Build CI
#      env:
#        HTTPS_PROXY: http://10.240.58.39:3128 # SQUID PROXY
#        HTTP_PROXY: http://10.240.58.39:3128 # SQUID PROXY
#        NO_PROXY: localhost,0.0.0.0,127.0.0.1,10.244.0.0/16,192.168.0.0/16,10.129.178.173,nexus.telefonica.com.br,acrsharedservices01.azurecr.io,appcs-azdevops-shared.azconfig.io,scm.azurewebsites.net

#    - script: npm pack
#      displayName: Pack

    # fix: Aparentemente o agente tem preconfigurado um .npmrc que exige essa variavel ${NEXUS_USR}
    - script: echo "##vso[task.setvariable variable=NEXUS_USR]$(NEXUS-DEFAULT-USR)"
      displayName: Set NEXUS_USR Var

    - script: |
        cat >> $(System.ArtifactsDirectory)/bin/.npmrc <<EOF
        _auth=$(echo -n $(NEXUS-DEFAULT-USR):$(NEXUS-DEFAULT-PSW) | base64)
        legacy-peer-deps=true
        EOF
      displayName: Write .npmrc

    - script: npm publish -ddd --registry https://nexus.telefonica.com.br/repository/novoportalgf-npm/
      workingDirectory: $(System.ArtifactsDirectory)/bin
      displayName: Publish


