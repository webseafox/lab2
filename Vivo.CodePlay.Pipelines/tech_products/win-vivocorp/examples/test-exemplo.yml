# Teste do exemplo.bat no Pipeline

trigger: none

resources:
  repositories:
    - repository: Vivo.CodePlay.Pipelines
      name: DevOps/Vivo.CodePlay.Pipelines
      type: git
      ref: refs/heads/master

extends:
  template: /tech_products/win-vivocorp/prod-new.yml@Vivo.CodePlay.Pipelines
  parameters:
    command: 'exemplo.bat'
    environment: 'prod'
    timeout: 30000
