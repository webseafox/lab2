parameters:
- name: environment 
  type: string

variables:
- template: variables/load.yml
  parameters:
    environment: ${{parameters.environment}}

stages:
- template: stages/backup/identification.yml

- template: stages/backup/register.yml

- template: stages/backup/export_adm.yml