variables:
- template: /tech_products/salesforce-automation/provisioners/salesforce/default/variables/load.yml

resources:
  repositories:
    - repository: FortifyExclusion
      name: DevOps/Vivo.Fortify.Exclusions.Salesforce
      type: git
      ref: refs/heads/master
      endpoint: CorePipelines
    - repository: CorePipelines
      name: DevOps/Vivo.Core.Pipelines
      type: git
      ref: refs/tags/v1
      endpoint: CorePipelines

stages:
- template: /tech_products/salesforce-automation/provisioners/salesforce/default/stages/security.yml
  parameters:
    fortifyExclusionRepository: FortifyExclusion
    ${{ if eq(variables.skip_quality_gates, true) }}:
      dependsOn: []
