parameters:
- name: comando_maven
  type: string

stages:
- stage: execucao
  displayName: "Execucao"
  dependsOn:
    - preparacao  
  jobs:
  - job: execucao
    pool:
      name: GeneralPurposeLinuxAgentsCI
    steps:
    - task: AzureKeyVault@2
      displayName: 'Recuperando credenciais'
      inputs:
        azureSubscription: DevOpsSharedResources
        KeyVaultName: kv-azdevops-shared
        secretsFilter: 'NEXUS-DEPS-USR, NEXUS-DEPS-PSW'
    - task: Docker@2
      displayName: 'Fazendo autenticação'
      inputs:
        command: 'login'
        containerRegistry: ACR-DEVOPS
        timeoutInMinutes: 90     
    - bash: |
        # Runs builder image
        docker run --rm \
          --user="$(id -u):$(id -g)" \
          --network host \
          --log-driver none \
          -e HTTPS_PROXY="$HTTPS_PROXY" \
          -e HTTP_PROXY="$HTTP_PROXY" \
          -e NO_PROXY="$NO_PROXY" \
          -e JAVA_OPTS_MEMORY_MAX="$DEFAULT_MAX_MEMORY_BUILD" \
          -e TZ="America/Sao_Paulo" \
          -e SYSTEM_ACCESSTOKEN="$SYSTEM_ACCESSTOKEN" \
          -e NEXUS_DEPS_USR="$NEXUS_DEPS_USR" \
          -e NEXUS_DEPS_PSW="$NEXUS_DEPS_PSW" \
          -v $(pwd):/app \
          -v $HOME/.m2:/.m2 \
          -w /app \
        acrsharedservices01.azurecr.io/base/qa/selenium-junit/oraclient/runtime:1.0.1 \
        /bin/bash -c "mvn clean test -U -X -f pom.xml -s /app/settings.xml -Djava.awt.headless=true -Dtest=CN3018_HabilitacaoDeUmaLinhaControlePJViaAPI.java -Doracle.jdbc.timezoneAsRegion=false -Dmaven.repo.local=/.m2/repository"
      displayName: Fazendo o build
      env:
        SYSTEM_ACCESSTOKEN: $(System.AccessToken)
        NEXUS_DEPS_USR: $(NEXUS-DEPS-USR)
        NEXUS_DEPS_PSW: $(NEXUS-DEPS-PSW)
        DEFAULT_MAX_MEMORY_BUILD: "$DEFAULT_MAX_MEMORY_BUILD"
        DOCKER_WORKDIR: $(DOCKER_WORKDIR)/app/